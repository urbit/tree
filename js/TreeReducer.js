import { createStore } from 'redux';

const initialState = {
  curr: '',
};

function tree(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default tree;
